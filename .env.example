# ACE-Step Local — environment config
# Copy to .env and adjust as needed.

# Port for the Flask server
PORT=5003

# Default models to use (must exist in models/)
# Fast defaults — swap filenames for higher quality
LM_MODEL=acestep-5Hz-lm-0.6B-Q8_0.gguf
DIT_MODEL=acestep-v15-turbo-Q4_K_M.gguf
